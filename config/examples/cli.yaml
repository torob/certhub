url: https://certhub.example.com
sync:
  wait: true
  timeout: 5m
  poll_interval: 10s
  force: false
  fail_fast: false
scheduler:
  interval: 6h
  jitter: 30s
  run_on_start: true
certificates:
  - domains:
      - example.com
    out_dir: /etc/certhub/material/example
