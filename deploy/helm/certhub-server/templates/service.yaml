apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-certhub-server
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: certhub-server
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: certhub-server
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
