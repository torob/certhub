# Certhub systemd examples

The units expect secrets to be supplied by root-owned environment files such as
`/etc/certhub/server.env` or `/etc/certhub/cli.env`. Do not put production
secrets in these example files or in the unit definitions.
